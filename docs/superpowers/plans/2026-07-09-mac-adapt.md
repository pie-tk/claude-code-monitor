# cc-console macOS 适配 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Windows 专用的 cc-console 适配到 macOS（universal），产出 ad-hoc 签名的 DMG 安装包，覆盖监控/桥接/内嵌终端/注入/启动/辅助功能/打包全链路。

**Architecture:** 沿用项目既有的 build tag 隔离架构，替换 darwin 存根为真实实现。纯 Go（无 cgo），用 `exec.Command` 调系统命令（ps/lsof/pgrep/defaults/osascript）；内嵌终端用 `creack/pty`；打包手动构造 `.app` bundle + `hdiutil`。前端基本不动（`Call.ByID` 平台无关）。

**Tech Stack:** Go 1.26 · Wails v3 alpha (WKWebView) · `github.com/creack/pty` · Vanilla JS + xterm.js · `hdiutil`/`iconutil`/`sips`/`codesign`。

## Global Constraints

- **目标平台**：macOS，universal 二进制（arm64 + amd64，`lipo` 合并）。
- **签名**：ad-hoc（`codesign --force --deep --sign -`），不做 Developer ID/公证。
- **纯 Go 无 cgo**：所有系统交互走 `exec.Command`，不得引入 cgo（否则破坏 `GOARCH=amd64` 交叉编译）。
- **build tag 策略**：darwin 专属走 `_darwin.go`；unix 通用走 `!windows`；新增 `_darwin.go` 时对应 `!windows` 存根 tag 收窄为 `!windows && !darwin`。
- **claude 是 node 进程**：检测/识别一律按**命令行**匹配（含 `claude` 的 node），不得用进程名 `claude.exe`；须排除 `/Applications/Claude.app`。
- **命名**：darwin 辅助二进制名 `cc-console-sl`（无 `.exe`）。
- **代码风格**：中文注释、英文标识符、内联短逻辑（沿用项目既有风格）。
- **分支**：所有改动在 `tmp/ai-develop/binky/2026-07-09` 分支；提交用 `git-commit` skill；commit message 类型 `A`（新增）/`O`（优化）。
- **设计依据**：`docs/superpowers/specs/2026-07-09-mac-adapt-design.md`。

## File Structure

| 文件 | 职责 | 所属 Task |
|---|---|---|
| `go.mod` / `go.sum` | 新增 `creack/pty` 依赖 | T1 |
| `internal/monitor/bridge.go` | `slhookExeName`/`hookCommandTag` 改 var + init 按 GOOS | T2 |
| `app.go` | 补 `Mac` 选项（ActivationPolicy） | T2 |
| `internal/monitor/detector_darwin.go` | ps/lsof/pgrep 进程检测 | T3 |
| `internal/monitor/bridge_other.go` | `processCmdline` 用 ps | T3 |
| `cmd/slhook/proctree_darwin.go` | `findClaudePID` ppid 链 | T4 |
| `cmd/slhook/proctree_other.go` | tag 收窄 | T4 |
| `cmd/slhook/bridge.mjs` | spawn 名按 platform | T4 |
| `internal/monitor/pty_unix.go`（改名自 pty_other.go） | creack/pty 实现 | T5 |
| `internal/monitor/pty.go` | 新增 `SessionByPID` | T5 |
| `service/monitor_service.go` | darwin 分支 buildCmdline | T5 |
| `internal/monitor/inject_darwin.go` | 内嵌注入 + 外部注入 | T6, T8 |
| `internal/monitor/launch_darwin.go` | 启动实例 | T7 |
| `internal/monitor/launch_other.go` | tag 收窄 | T7 |
| `internal/theme/detect_darwin.go` | defaults read 暗色 | T9 |
| `internal/monitor/settings_browser.go` | darwin `open` | T9 |
| `internal/monitor/settings_darwin.go` | LaunchAgent 自启 | T9 |
| `internal/monitor/settings_default.go` | tag 收窄 | T9 |
| `internal/monitor/update.go` | platform key 按 GOOS/GOARCH | T9 |
| `internal/monitor/update_darwin.go` | 下载 dmg | T9 |
| `frontend/src/main.js` | openNewTerminal kind | T10 |
| `frontend/index.html` | 文案 | T10 |
| `build-mac.sh` | universal + .app + ad-hoc + DMG | T11 |

---

## Task 1: 环境与依赖准备

**Files:**
- Modify: `go.mod`, `go.sum`

**Interfaces:**
- Produces: 可用的 Go 工具链 + `creack/pty` 依赖 + 当前代码在 mac 能编译。

- [ ] **Step 1: 安装 Go 1.26+**

```bash
brew install go
go version
```
Expected: `go version go1.26.x darwin/arm64`（版本号 ≥ 1.26）。

- [ ] **Step 2: 验证当前代码在 mac 编译通过（存根应能编译）**

```bash
cd /Users/binky/Code/AICoding/work/2026-07-09-cc-console-mac/cc-console
go build ./...
```
Expected: 无报错（darwin 存根保证编译）。若报错，先记录报错信息——可能是某个 `!windows` 文件在 darwin 缺符号，后续 Task 修复。

- [ ] **Step 3: 安装前端依赖并验证构建**

```bash
cd frontend && npm install && npm run build && cd ..
ls frontend/dist/index.html 2>/dev/null || ls frontend/dist/assets
```
Expected: `dist/` 下生成 `index.html` 或 `assets/`（Vite 产物）。

- [ ] **Step 4: 添加 creack/pty 依赖**

```bash
go get github.com/creack/pty
go mod tidy
grep creack/pty go.mod
```
Expected: go.mod 出现 `github.com/creack/pty v...`。

- [ ] **Step 5: 验证仍可编译**

```bash
go build ./...
```
Expected: 无报错。

- [ ] **Step 6: Commit**

```bash
git add go.mod go.sum
```
然后用 `git-commit` skill 提交（类型 `O`，描述「新增 creack/pty 依赖用于 macOS 内嵌终端」）。

---

## Task 2: bridge.go 平台常量 + app.go Mac 选项

**Files:**
- Modify: `internal/monitor/bridge.go:74-80`（const→var + init）
- Modify: `app.go:23-47`（补 Mac 选项）
- Test: `internal/monitor/bridge_platform_test.go`

**Interfaces:**
- Produces: `bridge.go` 的 `slhookExeName`/`hookCommandTag` 在 darwin 为 `cc-console-sl`；`app.go` 在 mac 显示 Dock 图标。

- [ ] **Step 1: 写失败测试（平台常量按 GOOS）**

`internal/monitor/bridge_platform_test.go`:
```go
//go:build !windows

package monitor

import "testing"

func TestSlhookExeNameNoExeOnUnix(t *testing.T) {
	if slhookExeName == "cc-console-sl.exe" {
		t.Fatalf("非 Windows 平台 slhookExeName 不应带 .exe，实际: %s", slhookExeName)
	}
	if hookCommandTag == "cc-console-sl.exe" {
		t.Fatalf("非 Windows 平台 hookCommandTag 不应带 .exe，实际: %s", hookCommandTag)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test -run TestSlhookExeNameNoExeOnUnix ./internal/monitor/
```
Expected: FAIL（当前 const 仍是 `.exe`）。注：此测试 build tag `!windows`，在 mac 运行。

- [ ] **Step 3: 改 bridge.go const→var + init 分支**

把 `internal/monitor/bridge.go:74-80` 的：
```go
const (
	slhookExeName  = "cc-console-sl.exe"
	bridgeMjsName  = "bridge.mjs"
	slhookMarker   = "bridge.mjs"
	hookModeArg    = "--hook"
	hookCommandTag = "cc-console-sl.exe"
)
```
改为：
```go
const (
	bridgeMjsName = "bridge.mjs"
	slhookMarker  = "bridge.mjs" // statusLine.command 含此串即表示已由我们接管(node 入口)
	hookModeArg   = "--hook"
)

// slhookExeName / hookCommandTag 平台相关：Windows 为 cc-console-sl.exe，其它平台无 .exe。
// 用 var + init 分支（const 无法按运行时平台取值）。
var (
	slhookExeName  = "cc-console-sl.exe"
	hookCommandTag = "cc-console-sl.exe"
)

func init() {
	if runtime.GOOS != "windows" {
		slhookExeName = "cc-console-sl"
		hookCommandTag = "cc-console-sl"
	}
}
```
并在 bridge.go import 块加入 `"runtime"`。

- [ ] **Step 4: 运行测试确认通过**

```bash
go test -run TestSlhookExeNameNoExeOnUnix ./internal/monitor/
```
Expected: PASS。

- [ ] **Step 5: 补 app.go Mac 选项**

读 `app.go` 确认 `application.Options` 是否有 `Mac` 字段：
```bash
grep -rn "MacOptions\|ActivationPolicy\|type Options" $(go env GOMODCACHE)/github.com/wailsapp/wails/v3@*/pkg/application/*.go | head -20
```
根据实际 API，在 `app.go` 的 `application.New(application.Options{...})` 内补 `Mac` 字段。若 API 为 `Mac *application.MacOptions`：
```go
Mac: &application.MacOptions{
	ApplicationName:    "CC Console",
	ActivationPolicy:   application.ActivationPolicyRegular,
},
```
若该 alpha 版字段名不同（如无 ActivationPolicy），则仅设 `ApplicationName`，并在 Step 7 验收时确认 Dock 图标显示；显示异常再调整。**以编译通过为准**——编译失败说明字段名错，查实际 API 修正。

- [ ] **Step 6: 验证编译 + 启动**

```bash
go build ./...
go run . &
sleep 3
# 观察：Dock 是否出现 CC Console 图标；托盘是否有 CC Console
kill %1 2>/dev/null
```
Expected: 编译通过；app 启动后 Dock 有图标、托盘有 CC Console（窗口可能空白因为 detector 存根，无妨）。

- [ ] **Step 7: Commit**

```bash
git add internal/monitor/bridge.go internal/monitor/bridge_platform_test.go app.go
```
用 `git-commit` skill（类型 `O`，描述「bridge 平台常量按 GOOS 分支 + app.go Mac 选项」）。

---

## Task 3: 进程检测 detector_darwin.go

**Files:**
- Modify(Rewrite): `internal/monitor/detector_darwin.go`
- Modify: `internal/monitor/bridge_other.go`
- Test: `internal/monitor/detector_darwin_test.go`

**Interfaces:**
- Consumes: detector.go 声明的 `isProcessAlive func(int,int64) bool` / `enumerateClaude func() []claudeProc` / `procCwd func(int) string` / `enumerateChildren func([]int) map[int][]int`；`claudeProc{pid int; createMs int64}`；bridge.go 的 `processCmdline func(int) string`。
- Produces: `init()` 注册上述 4 个函数变量 + processCmdline；`go run . --list` 能枚举本机 claude 进程。

- [ ] **Step 1: 写失败测试（ps 输出解析 + claude 匹配 + 排除 Desktop）**

`internal/monitor/detector_darwin_test.go`:
```go
//go:build darwin

package monitor

import "testing"

func TestParsePsLineMatchesClaudeNode(t *testing.T) {
	cases := []struct {
		line   string
		match  bool
	}{
		{"12345 node /Users/x/.nvm/versions/node/v25/bin/node /Users/x/.nvm/.../claude/cli.js", true},
		{"12345 /usr/local/bin/node /path/to/claude", true},
		{"12346 /Applications/Claude.app/Contents/MacOS/Claude", false},          // 排除 Desktop
		{"12347 /bin/zsh", false},                                                 // 非 claude
		{"12348 node /path/to/something/else", false},                             // node 但非 claude
	}
	for _, c := range cases {
		got := isClaudeCmdline(c.line)
		if got != c.match {
			t.Errorf("isClaudeCmdline(%q)=%v want %v", c.line, got, c.match)
		}
	}
}

func TestIsClaudeCmdlineCaseInsensitive(t *testing.T) {
	if !isClaudeCmdline("1 node /x/Claude/cli.js") {
		t.Fatal("应大小写不敏感匹配 claude")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test -run TestParsePs -race ./internal/monitor/ 2>&1 | head
go test -run TestIsClaudeCmdline -race ./internal/monitor/ 2>&1 | head
```
Expected: FAIL（`isClaudeCmdline` 未定义）。

- [ ] **Step 3: 重写 detector_darwin.go**

完整内容：
```go
//go:build darwin

package monitor

import (
	"fmt"
	"os"
	"os/exec"
	"path/filepath"
	"strconv"
	"strings"
	"syscall"
	"time"
)

func init() {
	isProcessAlive = darwinIsProcessAlive
	enumerateClaude = darwinEnumerateClaude
	procCwd = darwinProcCwd
	enumerateChildren = darwinEnumerateChildren
}

// isClaudeCmdline 判断一行 ps 命令行是否为「跑 claude 的 node 进程」。
// 规则：包含 "node" 且路径/参数含 "claude"，且不含 Claude Desktop.app 路径。大小写不敏感。
func isClaudeCmdline(line string) bool {
	low := strings.ToLower(line)
	if !strings.Contains(low, "claude") {
		return false
	}
	if strings.Contains(low, "/applications/claude.app/") {
		return false
	}
	// 必须是 node 进程（claude CLI 是 node 脚本）
	return strings.Contains(low, "node")
}

// darwinEnumerateClaude 用 ps -ax 枚举所有进程命令行，筛出 claude(node)。
func darwinEnumerateClaude() []claudeProc {
	out, err := exec.Command("ps", "-ax", "-o", "pid=,command=").Output()
	if err != nil {
		return nil
	}
	var procs []claudeProc
	for _, line := range strings.Split(string(out), "\n") {
		line = strings.TrimSpace(line)
		if line == "" {
			continue
		}
		// 格式："PID COMMAND..."（PID 与命令间多空格）
		sp := strings.SplitN(line, " ", 2)
		if len(sp) < 2 {
			continue
		}
		pid, err := strconv.Atoi(strings.TrimSpace(sp[0]))
		if err != nil || pid <= 0 {
			continue
		}
		cmd := strings.TrimSpace(sp[1])
		if !isClaudeCmdline(cmd) {
			continue
		}
		procs = append(procs, claudeProc{pid: pid, createMs: darwinProcCreateMs(pid)})
	}
	return procs
}

// darwinProcCwd 用 lsof 读 pid 的 cwd。
func darwinProcCwd(pid int) string {
	out, err := exec.Command("lsof", "-a", "-p", strconv.Itoa(pid), "-d", "cwd", "-Fn").Output()
	if err != nil {
		return ""
	}
	for _, line := range strings.Split(string(out), "\n") {
		// lsof -n 输出 cwd 行形如 "n/Users/x/project"
		if strings.HasPrefix(line, "n") {
			return strings.TrimPrefix(line, "n")
		}
	}
	return ""
}

// darwinIsProcessAlive 检查 pid 存在且启动时间与 startedAt 匹配（容差 15s，防 pid 复用）。
func darwinIsProcessAlive(pid int, startedAt int64) bool {
	proc, err := os.FindProcess(pid)
	if err != nil {
		return false
	}
	// signal 0 探活
	if err := proc.Signal(syscall.Signal(0)); err != nil {
		return false
	}
	if startedAt == 0 {
		return true
	}
	createMs := darwinProcCreateMs(pid)
	if createMs == 0 {
		return true // 取不到时间则只信存活
	}
	diff := createMs - startedAt
	if diff < 0 {
		diff = -diff
	}
	return diff <= 15000
}

// darwinProcCreateMs 用 ps -o lstart= 解析 pid 启动时间为 epoch 毫秒。失败返回 0。
func darwinProcCreateMs(pid int) int64 {
	out, err := exec.Command("ps", "-o", "lstart=", "-p", strconv.Itoa(pid)).Output()
	if err != nil {
		return 0
	}
	s := strings.TrimSpace(string(out))
	if s == "" {
		return 0
	}
	// lstart 格式："Mon Jan  2 15:04:05 2026"
	t, err := time.Parse("Mon Jan _2 15:04:05 2006", s)
	if err != nil {
		// 回退：尝试不带前导空格占位的格式
		t, err = time.Parse("Mon Jan 2 15:04:05 2006", s)
		if err != nil {
			return 0
		}
	}
	return t.UnixMilli()
}

// darwinEnumerateChildren 用 pgrep -P 枚举每个 claude pid 的直接子进程。
func darwinEnumerateChildren(claudePids []int) map[int][]int {
	out := make(map[int][]int, len(claudePids))
	for _, p := range claudePids {
		raw, err := exec.Command("pgrep", "-P", strconv.Itoa(p)).Output()
		if err != nil {
			continue
		}
		for _, line := range strings.Split(string(raw), "\n") {
			line = strings.TrimSpace(line)
			if line == "" {
				continue
			}
			if child, err := strconv.Atoi(line); err == nil {
				out[p] = append(out[p], child)
			}
		}
	}
	return out
}

// 保留 filepath 引用避免未用（darwinProcCwd 路径处理可能扩展）
var _ = filepath.Join
var _ = fmt.Sprintf
```

- [ ] **Step 4: 改 bridge_other.go 的 processCmdline**

读 `internal/monitor/bridge_other.go`，把 `processCmdline = func(pid int) string { return "" }` 改为真实实现：
```go
processCmdline = func(pid int) string {
	out, err := exec.Command("ps", "-o", "command=", "-p", strconv.Itoa(pid)).Output()
	if err != nil {
		return ""
	}
	return strings.ToLower(strings.TrimSpace(string(out)))
}
```
确保 import 含 `os/exec`、`strconv`、`strings`（按现有 import 增补）。

- [ ] **Step 5: 运行测试确认通过**

```bash
go test -race ./internal/monitor/
```
Expected: PASS。

- [ ] **Step 6: 手动验证（启动一个 claude 后枚举）**

新开终端跑 `claude`（进入任意目录），然后：
```bash
cd /Users/binky/Code/AICoding/work/2026-07-09-cc-console-mac/cc-console
go run . --list
```
Expected: 表格列出刚启动的 claude 实例（PID、工作目录）。若工作目录为空，说明 lsof 未取到 cwd——检查 `lsof -a -p <pid> -d cwd -Fn` 实际输出格式，调整 `darwinProcCwd` 解析。

- [ ] **Step 7: Commit**

```bash
git add internal/monitor/detector_darwin.go internal/monitor/detector_darwin_test.go internal/monitor/bridge_other.go
```
用 `git-commit` skill（类型 `A`，描述「macOS 进程检测：ps 枚举 claude(node)/lsof 读 cwd/pgrep 子进程」）。

---

## Task 4: slhook proctree_darwin.go + bridge.mjs spawn 名

**Files:**
- Create: `cmd/slhook/proctree_darwin.go`
- Modify: `cmd/slhook/proctree_other.go`（tag 收窄）
- Modify: `cmd/slhook/bridge.mjs`
- Test: `cmd/slhook/proctree_darwin_test.go`

**Interfaces:**
- Consumes: `findClaudePID() int`（proctree 契约）。
- Produces: darwin 的 `findClaudePID` 沿 ppid 链找到 claude(node)；bridge.mjs 在 mac spawn `cc-console-sl`。

- [ ] **Step 1: 写失败测试（ppid 链遍历逻辑）**

`cmd/slhook/proctree_darwin_test.go`:
```go
//go:build darwin

package main

import "testing"

func TestIsClaudeNodeCommand(t *testing.T) {
	cases := []struct{ cmd string; want bool }{
		{"node /x/.nvm/.../claude/cli.js", true},
		{"node /x/claude", true},
		{"/Applications/Claude.app/Contents/MacOS/Claude", false},
		{"/bin/zsh", false},
	}
	for _, c := range cases {
		if got := isClaudeNodeCommand(c.cmd); got != c.want {
			t.Errorf("isClaudeNodeCommand(%q)=%v want %v", c.cmd, got, c.want)
		}
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test -run TestIsClaudeNodeCommand ./cmd/slhook/
```
Expected: FAIL（未定义）。

- [ ] **Step 3: 创建 proctree_darwin.go**

`cmd/slhook/proctree_darwin.go`:
```go
//go:build darwin

package main

import (
	"os"
	"os/exec"
	"strconv"
	"strings"
)

// findClaudePID 从自身进程起沿父进程链向上查找「跑 claude 的 node 进程」，返回其 pid。
// 链路：claude(node) → statusline: node bridge.mjs → cc-console-sl(本进程)。
// 向上最多追溯 8 级。未找到返回 0。
func findClaudePID() int {
	pid := os.Getpid()
	for range 8 {
		ppid := parentPID(pid)
		if ppid <= 1 {
			break
		}
		if cmd := psCommand(ppid); isClaudeNodeCommand(cmd) {
			return ppid
		}
		pid = ppid
	}
	return 0
}

// isClaudeNodeCommand 判断命令行是否为跑 claude 的 node 进程。
func isClaudeNodeCommand(cmd string) bool {
	low := strings.ToLower(cmd)
	if !strings.Contains(low, "node") {
		return false
	}
	if !strings.Contains(low, "claude") {
		return false
	}
	return !strings.Contains(low, "/applications/claude.app/")
}

// parentPID 返回 pid 的父 pid，失败返回 0。
func parentPID(pid int) int {
	out, err := exec.Command("ps", "-o", "ppid=", "-p", strconv.Itoa(pid)).Output()
	if err != nil {
		return 0
	}
	p, err := strconv.Atoi(strings.TrimSpace(string(out)))
	if err != nil {
		return 0
	}
	return p
}

// psCommand 返回 pid 的完整命令行，失败返回 ""。
func psCommand(pid int) string {
	out, err := exec.Command("ps", "-o", "command=", "-p", strconv.Itoa(pid)).Output()
	if err != nil {
		return ""
	}
	return strings.TrimSpace(string(out))
}
```

- [ ] **Step 4: 收窄 proctree_other.go 的 build tag**

把 `cmd/slhook/proctree_other.go` 顶部的 `//go:build !windows` 改为：
```go
//go:build !windows && !darwin
```
（这样 darwin 用 proctree_darwin.go，linux 仍用存根。）

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./cmd/slhook/
go build ./cmd/slhook/  # 确认无重复定义
```
Expected: PASS + 编译无 "findClaudePID redeclared"。

- [ ] **Step 6: 改 bridge.mjs spawn 名**

读 `cmd/slhook/bridge.mjs`，找到 spawn `cc-console-sl.exe` 的行（约 L29），把硬编码名改为按 platform：
```javascript
const slhookName = process.platform === 'win32' ? 'cc-console-sl.exe' : 'cc-console-sl';
const slhook = path.join(path.dirname(process.argv[1]), slhookName);
```
（保持其余逻辑不变。）

- [ ] **Step 7: 手动验证（live 文件写入）**

```bash
cd /Users/binky/Code/AICoding/work/2026-07-09-cc-console-mac/cc-console
go build -o /tmp/cc-console-sl ./cmd/slhook
# 模拟 bridge.mjs 调用：用 slhook 读 stdin 并写 live 文件
echo '{"pid":0,"model":"test","cwd":"/tmp"}' | /tmp/cc-console-sl
ls ~/.cc-console/live/
```
Expected: `~/.cc-console/live/` 下出现 `<某pid>.json`（pid 为 slhook 向上找到的 claude；若当前无 claude 运行则为 0 或无文件——属正常，验证逻辑不 panic 即可）。再用真实 claude + statusline 链路端到端验证（见 Task 5 后的集成验证）。

- [ ] **Step 8: Commit**

```bash
git add cmd/slhook/proctree_darwin.go cmd/slhook/proctree_darwin_test.go cmd/slhook/proctree_other.go cmd/slhook/bridge.mjs
```
用 `git-commit` skill（类型 `A`，描述「slhook macOS 进程树遍历 + bridge.mjs spawn 名按平台」）。

---

## Task 5: 内嵌终端 pty_unix.go（creack/pty）

**Files:**
- Rename: `internal/monitor/pty_other.go` → `internal/monitor/pty_unix.go`（重写内容）
- Modify: `internal/monitor/pty.go`（新增 `SessionByPID`）
- Modify: `service/monitor_service.go:320-399`（buildCmdline darwin 分支）
- Test: `internal/monitor/pty_unix_test.go`

**Interfaces:**
- Consumes: `PTYRegistry`（pty.go）；`StartPTYSession(kind, workdir, cmdline string, cols, rows int, onData func(string), onExit func(int)) (string, error)` 契约；`TerminalInfo{ID,PID,Kind,Workdir,Live}`。
- Produces: `ptySession` 平台类型；`ConPTYSupported() bool`；`ptyRegistry.SessionByPID(pid uint32) (id string, ok bool)`；`StartPTYSession` 真实实现；darwin 下 `go run .` 能开内嵌 shell tab。

- [ ] **Step 1: 写失败测试（SessionByPID）**

`internal/monitor/pty_unix_test.go`:
```go
//go:build !windows

package monitor

import "testing"

func TestSessionByPIDEmptyRegistry(t *testing.T) {
	r := NewPTYRegistry()
	if id, ok := r.SessionByPID(12345); ok || id != "" {
		t.Fatalf("空注册表不应命中，got id=%q ok=%v", id, ok)
	}
}

func TestConPTYSupportedOnUnix(t *testing.T) {
	if !ConPTYSupported() {
		t.Fatal("unix 平台 ConPTYSupported 应为 true")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test -run "TestSessionByPIDEmptyRegistry|TestConPTYSupportedOnUnix" ./internal/monitor/
```
Expected: FAIL（`SessionByPID` 未定义；`ConPTYSupported` 当前返回 false）。

- [ ] **Step 3: 给 pty.go 加 SessionByPID + 进程级单例**

**关键架构（务必照做，否则注入静默失效）**：`PTYRegistry` 实例当前由 service 层私有持有（`NewMonitorService` 内 `monitor.NewPTYRegistry()`，见 `monitor_service.go:27`）。但注入层 `inject_darwin.go`（同在 monitor 包）需按 pid 反查并写 PTY master，必须访问**同一个**实例。因此新增 monitor 包级单例 `ptyRegistry` + getter，service 改用 getter、inject 直接用包级变量——三方共享同一实例。若 service 仍用 `NewPTYRegistry()` 自建实例，则 inject 命中的包级单例是空注册表，所有内嵌注入静默失效。

在 `internal/monitor/pty.go` 的 `NewPTYRegistry` 函数（`:29-31`）之后追加（进程级单例 + getter）：
```go
// ptyRegistry 是进程级唯一的内置终端注册表。service 层（管理会话）与 inject 层（按 pid 注入）
// 共享同一实例：service 用 GetPTYRegistry() 拿引用，inject_darwin 用包级 ptyRegistry 直接访问。
var ptyRegistry = NewPTYRegistry()

// GetPTYRegistry 返回进程级内置终端注册表。
func GetPTYRegistry() *PTYRegistry { return ptyRegistry }
```

并在 `internal/monitor/pty.go` 的 `CloseAll` 之后追加（pid 反查方法）：
```go
// SessionByPID 按 pid 反查 session id（遍历各 session 的 info().PID）。供注入层判断某 pid 是否内嵌实例。
func (r *PTYRegistry) SessionByPID(pid uint32) (string, bool) {
	r.mu.RLock()
	defer r.mu.RUnlock()
	for _, s := range r.sessions {
		if s.info().PID == pid {
			return s.id, true
		}
	}
	return "", false
}
```

- [ ] **Step 4: 重写 pty_unix.go（改名 + creack/pty 实现）**

先删除旧 `pty_other.go`，创建 `internal/monitor/pty_unix.go`：
```bash
git mv internal/monitor/pty_other.go internal/monitor/pty_unix.go
```
完整内容：
```go
//go:build !windows

package monitor

import (
	"fmt"
	"io"
	"os"
	"os/exec"
	"sync"

	"github.com/creack/pty"
)

// ptySession 是 unix 平台的内置终端会话：creack/pty 的 master fd + 子进程。
type ptySession struct {
	id      string
	kind    string
	workdir string
	master  *os.File
	cmd     *exec.Cmd
	mu      sync.Mutex
	done    chan struct{}
}

func (s *ptySession) info() TerminalInfo {
	pid := uint32(0)
	if s.cmd != nil && s.cmd.Process != nil {
		pid = uint32(s.cmd.Process.Pid)
	}
	return TerminalInfo{
		ID:      s.id,
		PID:     pid,
		Kind:    s.kind,
		Workdir: s.workdir,
		Live:    true,
	}
}

// ConPTYSupported unix 平台内置终端可用。
func ConPTYSupported() bool { return true }

// StartPTYSession 用 creack/pty 启动子进程，返回 session id。
// cmdline 作为 shell 命令交给 /bin/sh -c 执行（对应 Windows 的 cmd.exe /c 包装）。
func (r *PTYRegistry) StartPTYSession(kind, workdir, cmdline string, cols, rows int,
	onData func(string), onExit func(int)) (string, error) {

	if cmdline == "" {
		return "", fmt.Errorf("命令行为空")
	}
	cmd := exec.Command("/bin/sh", "-c", cmdline)
	cmd.Env = os.Environ()
	if workdir != "" {
		cmd.Dir = workdir
	}

	size := &pty.Winsize{Rows: uint16(rows), Cols: uint16(cols)}
	master, err := pty.StartWithSize(cmd, size)
	if err != nil {
		return "", fmt.Errorf("启动 PTY 失败: %w", err)
	}

	s := &ptySession{
		id:      r.nextID(),
		kind:    kind,
		workdir: workdir,
		master:  master,
		cmd:     cmd,
		done:    make(chan struct{}),
	}
	r.addSession(s)

	// 输出读取：阻塞读 master，子进程退出后 EOF 自然结束。
	go func() {
		buf := make([]byte, 4096)
		for {
			n, err := master.Read(buf)
			if n > 0 && onData != nil {
				onData(string(buf[:n]))
			}
			if err != nil {
				break
			}
		}
	}()

	// 退出检测：cmd.Wait 返回后 emit exit 并清理。
	go func() {
		err := cmd.Wait()
		code := 0
		if err != nil {
			if exitErr, ok := err.(*exec.ExitError); ok {
				code = exitErr.ExitCode()
			} else {
				code = -1
			}
		}
		close(s.done)
		_ = master.Close()
		if onExit != nil {
			onExit(code)
		}
	}()

	return s.id, nil
}

func (s *ptySession) Write(data []byte) error {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.master == nil {
		return fmt.Errorf("终端已关闭")
	}
	_, err := s.master.Write(data)
	return err
}

func (s *ptySession) Resize(cols, rows int) error {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.master == nil {
		return nil
	}
	return pty.Setsize(s.master, &pty.Winsize{Rows: uint16(rows), Cols: uint16(cols)})
}

// Close 终止子进程并关闭 master（幂等）。
func (s *ptySession) Close() {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.cmd != nil && s.cmd.Process != nil {
		_ = s.cmd.Process.Kill()
	}
	if s.master != nil {
		_ = s.master.Close()
	}
}

// 保留 io 引用（未来扩展用）
var _ = io.Copy
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test -race ./internal/monitor/
```
Expected: PASS。

- [ ] **Step 6: 给 monitor_service.go 的 buildCmdline 加 darwin 分支**

读 `service/monitor_service.go:320-399`，把 `buildTerminalCmdline` 改为含 darwin 分支。用 `runtime.GOOS`（需 import `runtime`）：

```go
func buildTerminalCmdline(kind, workdir string) (string, error) {
	if runtime.GOOS == "darwin" {
		return buildTerminalCmdlineDarwin(kind)
	}
	// ---- 既有 Windows 逻辑保持不变 ----
	switch kind {
	case "claude":
		return buildClaudeCmdline()
	case "pwsh":
		return fmt.Sprintf(`"%s" -NoLogo`, monitor.ResolveShellExe()), nil
	case "cmd":
		return `cmd.exe /K chcp 65001 >nul`, nil
	case "shell", "":
		return fmt.Sprintf(`"%s" -NoLogo`, monitor.ResolveShellExe()), nil
	default:
		return "", fmt.Errorf("未知终端类型: %s", kind)
	}
}

// buildTerminalCmdlineDarwin macOS 终端命令行（交给 /bin/sh -c 执行）。
func buildTerminalCmdlineDarwin(kind string) (string, error) {
	switch kind {
	case "claude":
		return buildClaudeCmdline()
	case "shell", "":
		sh := monitor.ResolveShellExe()
		if sh == "" {
			sh = "/bin/zsh"
		}
		return sh, nil
	default:
		return "", fmt.Errorf("macOS 不支持终端类型: %s", kind)
	}
}
```

把 `buildClaudeCmdline` 改为含 darwin 分支（在函数开头）：
```go
func buildClaudeCmdline() (string, error) {
	args := monitor.BuildClaudeArgsForPTY()
	argSuffix := ""
	if args != "" {
		argSuffix = " " + args
	}
	if runtime.GOOS == "darwin" {
		// macOS：claude 是 node 脚本，LookPath 直接命中（无 .cmd 包装器）。
		if p, err := exec.LookPath("claude"); err == nil {
			return fmt.Sprintf("%s%s", p, argSuffix), nil
		}
		return "", fmt.Errorf("未找到 claude 可执行文件，请确认 Claude Code 已安装且位于 PATH")
	}
	// ---- 既有 Windows 逻辑保持不变 ----
	// ...（原 1.收集 claude.cmd / 2.回退 raw claude.exe 不动）
```
（保留原 Windows 逻辑体在 darwin 分支之后。）确保 import `runtime`。

然后改 `NewMonitorService`（`monitor_service.go:26-28`），让它使用 Step 3 新增的包级单例（与注入层共享同一 PTYRegistry 实例）：
```go
// NewMonitorService 创建服务实例。
func NewMonitorService() *MonitorService {
	return &MonitorService{pty: monitor.GetPTYRegistry()}
}
```
（仅把原 `monitor.NewPTYRegistry()` 改为 `monitor.GetPTYRegistry()`。`s.pty` 字段类型仍为 `*monitor.PTYRegistry`，其余用法不变。若漏改此项，注入层访问的包级单例为空，所有内嵌注入静默失效。）

- [ ] **Step 7: 手动验证（开内嵌 shell tab）**

```bash
go build ./... && go run . &
sleep 3
```
在 GUI 窗口点「新建终端」开一个 shell tab，输入 `ls`、`echo hi`。
Expected: xterm 面板显示 shell 提示符，命令正常执行，输出回流。

- [ ] **Step 8: Commit**

```bash
git add internal/monitor/pty_unix.go internal/monitor/pty.go internal/monitor/pty_unix_test.go service/monitor_service.go
# 若 pty_other.go 已 git mv，一并 add
```
用 `git-commit` skill（类型 `A`，描述「macOS 内嵌终端 creack/pty 实现 + PTYRegistry.SessionByPID + darwin 命令行分支」）。

---

## Task 6: 内嵌实例命令注入 inject_darwin.go

**Files:**
- Modify(Rewrite): `internal/monitor/inject_darwin.go`
- Test: `internal/monitor/inject_darwin_test.go`

**Interfaces:**
- Consumes: `ConsoleInput` 接口（inject.go）；包级 `ptyRegistry`（T5 Step 3 进程级单例）的 `SessionByPID(pid uint32) (string,bool)`、`Write(id string, data []byte) error`、`Kill(id string)`。inject 与 service 共享此单例，故能命中 service 创建的内嵌会话。
- Produces: `init()` 注册 `Injector = &darwinInjector{}`；内嵌实例的 clear/rewind/prompt/ask/close 可用。

- [ ] **Step 1: 写失败测试（keyTokenBytes 映射 + actionToken 解析）**

`internal/monitor/inject_darwin_test.go`:
```go
//go:build darwin

package monitor

import (
	"encoding/json"
	"testing"
)

func TestKeyTokenBytes(t *testing.T) {
	cases := []struct {
		key  string
		want string
	}{
		{"up", "\x1b[A"},
		{"down", "\x1b[B"},
		{"right", "\x1b[C"},
		{"left", "\x1b[D"},
		{"enter", "\r"},
		{"esc", "\x1b"},
		{"tab", "\t"},
		{"space", " "},
		{"backspace", "\x7f"},
		{"delete", "\x1b[3~"},
		{"ctrl+a", "\x01"},
		{"ctrl+u", "\x15"},
		{"ctrl+k", "\x0b"},
		{"clearInput", "\x15"}, // ctrl+u 清行
	}
	for _, c := range cases {
		got, ok := keyTokenBytes(c.key)
		if !ok {
			t.Errorf("keyTokenBytes(%q) 未识别", c.key)
			continue
		}
		if got != c.want {
			t.Errorf("keyTokenBytes(%q)=%q want %q", c.key, got, c.want)
		}
	}
}

func TestRenderAskTokens(t *testing.T) {
	actions := `[{"key":"down"},{"key":"enter"},{"text":"hi"}]`
	var toks []actionToken
	if err := json.Unmarshal([]byte(actions), &toks); err != nil {
		t.Fatal(err)
	}
	got, err := renderAskTokens(toks)
	if err != nil {
		t.Fatal(err)
	}
	want := "\x1b[B" + "\r" + "hi"
	if got != want {
		t.Errorf("renderAskTokens=%q want %q", got, want)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test -run "TestKeyTokenBytes|TestRenderAskTokens" ./internal/monitor/
```
Expected: FAIL（未定义）。

- [ ] **Step 3: 重写 inject_darwin.go**

完整内容：
```go
//go:build darwin

package monitor

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"
)

func init() {
	Injector = &darwinInjector{}
}

type darwinInjector struct{}

// actionToken 对应前端 buildAskSequence 输出的按键 token（与 inject_windows 一致）。
type actionToken struct {
	Key  string `json:"key,omitempty"`
	Text string `json:"text,omitempty"`
}

// keyTokenBytes 把单个控制键 token 翻译为 PTY 字节序列（unix 终端）。
func keyTokenBytes(key string) (string, bool) {
	switch key {
	case "up":
		return "\x1b[A", true
	case "down":
		return "\x1b[B", true
	case "right":
		return "\x1b[C", true
	case "left":
		return "\x1b[D", true
	case "enter":
		return "\r", true
	case "esc":
		return "\x1b", true
	case "tab":
		return "\t", true
	case "space":
		return " ", true
	case "backspace":
		return "\x7f", true
	case "delete":
		return "\x1b[3~", true
	case "ctrl+a":
		return "\x01", true
	case "ctrl+u":
		return "\x15", true
	case "ctrl+k":
		return "\x0b", true
	case "clearInput":
		return "\x15", true // ctrl+u 清当前行
	}
	return "", false
}

// renderAskTokens 把 token 序列渲染为待写入 PTY 的字节串。
func renderAskTokens(toks []actionToken) (string, error) {
	var sb strings.Builder
	for _, tk := range toks {
		if tk.Text != "" {
			sb.WriteString(tk.Text)
			continue
		}
		b, ok := keyTokenBytes(tk.Key)
		if !ok {
			return "", fmt.Errorf("未知按键 token: %q", tk.Key)
		}
		sb.WriteString(b)
	}
	return sb.String(), nil
}

// writePTY 向 pid 对应的内嵌实例 PTY 写字节。命中内嵌返回 true。
func writePTY(pid int, data string) (bool, error) {
	id, ok := ptyRegistry.SessionByPID(uint32(pid))
	if !ok {
		return false, nil // 非内嵌实例
	}
	return true, ptyRegistry.Write(id, []byte(data))
}

func (d *darwinInjector) SendClear(pid int) error {
	if ok, err := writePTY(pid, "/clear\r"); ok {
		return err
	}
	return fmt.Errorf("该实例非内嵌终端，外部终端不支持 clear 注入（建议用内嵌实例）")
}

func (d *darwinInjector) SendRewind(pid int) error {
	// Claude Code 中两次 ESC 等价于回溯（与 Windows 一致）
	if ok, err := writePTY(pid, "\x1b\x1b"); ok {
		return err
	}
	return fmt.Errorf("该实例非内嵌终端，外部终端不支持 rewind 注入")
}

func (d *darwinInjector) SendPrompt(pid int, text string) error {
	if ok, err := writePTY(pid, text); ok {
		if err != nil {
			return err
		}
		time.Sleep(50 * time.Millisecond) // 给 claude 消费文本的时间
		_, err = writePTY(pid, "\r")
		return err
	}
	return fmt.Errorf("该实例非内嵌终端，外部终端不支持 prompt 注入")
}

func (d *darwinInjector) SendAskAnswer(pid int, actions string) error {
	var toks []actionToken
	if err := json.Unmarshal([]byte(actions), &toks); err != nil {
		return fmt.Errorf("actions JSON 解析失败: %w", err)
	}
	// 两段式：先控制键、再文本，避免中文宽字符消费时序问题（与 Windows 经验一致）
	var pending strings.Builder
	flush := func(s string) error {
		if s == "" {
			return nil
		}
		ok, err := writePTY(pid, s)
		if !ok {
			return fmt.Errorf("该实例非内嵌终端，外部终端不支持 ask 注入")
		}
		if err != nil {
			return err
		}
		time.Sleep(50 * time.Millisecond)
		return nil
	}
	for _, tk := range toks {
		if tk.Text != "" {
			if err := flush(pending.String()); err != nil {
				return err
			}
			pending.Reset()
			if err := flush(tk.Text); err != nil {
				return err
			}
			continue
		}
		b, ok := keyTokenBytes(tk.Key)
		if !ok {
			return fmt.Errorf("未知按键 token: %q", tk.Key)
		}
		pending.WriteString(b)
	}
	return flush(pending.String())
}

func (d *darwinInjector) ShowWindow(pid int) error {
	// 内嵌实例：前端通过事件激活 xterm tab（这里无窗口引用，返回 nil 由前端处理）。
	if _, ok := ptyRegistry.SessionByPID(uint32(pid)); ok {
		return nil
	}
	// 外部实例：尽力 activate（见 Task 8 注入 showExternalWindow）
	return showExternalWindow(pid)
}

func (d *darwinInjector) CloseInstance(pid int) (string, error) {
	if pid <= 0 {
		return "", fmt.Errorf("PID 无效")
	}
	if id, ok := ptyRegistry.SessionByPID(uint32(pid)); ok {
		ptyRegistry.Kill(id)
		return fmt.Sprintf("已关闭内嵌实例 PID %d", pid), nil
	}
	// 外部实例：尽力终止进程（见 Task 8）
	return closeExternalInstance(pid)
}

// showExternalWindow / closeExternalInstance 在 Task 8 实现（外部终端注入）。
// 此处先提供占位返回，保证 Task 6 独立可编译；Task 8 替换为真实实现。
func showExternalWindow(pid int) error {
	return fmt.Errorf("外部终端 ShowWindow 尚未实现")
}

func closeExternalInstance(pid int) (string, error) {
	return "", fmt.Errorf("外部终端 CloseInstance 尚未实现")
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
go test -race ./internal/monitor/
```
Expected: PASS。

- [ ] **Step 5: 手动验证（内嵌 claude 注入）**

```bash
go run . &
sleep 3
```
在 GUI 用「内嵌」模式启动一个 claude 实例（设 LaunchWindowMode=embedded），进入后用消息框发一条 prompt / 点 clear。
Expected: 内嵌 claude 收到 prompt 并执行；clear 清空对话。

- [ ] **Step 6: Commit**

```bash
git add internal/monitor/inject_darwin.go internal/monitor/inject_darwin_test.go
```
用 `git-commit` skill（类型 `A`，描述「macOS 内嵌实例命令注入（写 PTY master）+ 按键字节映射」）。

---

## Task 7: 启动实例 launch_darwin.go

**Files:**
- Create: `internal/monitor/launch_darwin.go`
- Modify: `internal/monitor/launch_other.go`（tag 收窄）

**Interfaces:**
- Consumes: `ResolveClaudePath() (string, error)` / `LaunchClaudeInDir(workdir, mode string) (string, error)` / `ResolveShellExe() string` / `BuildClaudeArgsForPTY() string` 契约（launch_windows.go 定义）。
- Produces: darwin 下 `LaunchInstance` 的外部窗口路径可用（osascript 开 Terminal）。

- [ ] **Step 1: 读 launch_windows.go 确认 BuildClaudeArgsForPTY 行为**

```bash
grep -n "func ResolveClaudePath\|func LaunchClaudeInDir\|func ResolveShellExe\|func BuildClaudeArgsForPTY\|LaunchYolo\|bypassPermissions" internal/monitor/launch_windows.go
```
确认 `BuildClaudeArgsForPTY` 在 LaunchYolo 设置时返回 `--permission-mode bypassPermissions`，darwin 复用同逻辑（该函数若在 launch_windows.go 则需迁移或复制到 launch_darwin.go）。

- [ ] **Step 2: 创建 launch_darwin.go**

`internal/monitor/launch_darwin.go`:
```go
//go:build darwin

package monitor

import (
	"fmt"
	"os/exec"
	"strings"
)

// ResolveClaudePath 查找 claude 可执行文件，排除 Claude Desktop.app。
func ResolveClaudePath() (string, error) {
	p, err := exec.LookPath("claude")
	if err == nil {
		if !strings.Contains(strings.ToLower(p), "/applications/claude.app/") {
			return p, nil
		}
	}
	return "", fmt.Errorf("未找到 claude（请确认 Claude Code 已安装且位于 PATH）")
}

// BuildClaudeArgsForPTY 返回启动 claude 时的额外参数（Yolo 模式加 bypassPermissions）。
func BuildClaudeArgsForPTY() string {
	if GetSettings().LaunchYolo {
		return "--permission-mode bypassPermissions"
	}
	return ""
}

// ResolveShellExe 返回用户默认 shell（$SHELL 回退 /bin/zsh）。
func ResolveShellExe() string {
	if sh := envOr("SHELL"); sh != "" {
		return sh
	}
	return "/bin/zsh"
}

// LaunchClaudeInDir 在 Terminal.app 中打开 workdir 并启动 claude。
// mode 仅 "show"/"hide" 影响是否 activate（macOS 无最小化到任务栏概念，hide 仍 activate 但不强制置前）。
func LaunchClaudeInDir(workdir, mode string) (string, error) {
	claude, err := ResolveClaudePath()
	if err != nil {
		return "", err
	}
	args := BuildClaudeArgsForPTY()
	argPart := ""
	if args != "" {
		argPart = " " + args
	}
	// osascript：在新 Terminal 窗口执行 cd + claude
	script := fmt.Sprintf(`tell application "Terminal"
		activate
		do script "cd %s && %s%s"
	end tell", shellEscape(workdir), shellEscape(claude), argPart)
	cmd := exec.Command("osascript", "-e", script)
	if err := cmd.Run(); err != nil {
		return "", fmt.Errorf("启动 Terminal 失败: %w", err)
	}
	return "terminal", nil
}

// shellEscape 给路径加双引号转义（简单实现，路径含双引号时退一步）。
func shellEscape(p string) string {
	return "\"" + strings.ReplaceAll(p, "\"", "\\\"") + "\""
}

// envOr 读环境变量，空返回 ""。
func envOr(key string) string {
	return getenv(key)
}
```
注：`getenv` 若包内不存在则用 `os.Getenv`——把 `envOr` 实现改为直接 `os.Getenv`：
```go
func ResolveShellExe() string {
	if sh := os.Getenv("SHELL"); sh != "" {
		return sh
	}
	return "/bin/zsh"
}
```
（并 import `os`，删除 envOr/getenv 辅助。）

- [ ] **Step 3: 收窄 launch_other.go 的 build tag**

把 `internal/monitor/launch_other.go` 顶部 `//go:build !windows` 改为：
```go
//go:build !windows && !darwin
```

- [ ] **Step 4: 验证编译**

```bash
go build ./...
```
Expected: 无 "redeclared" 错误。若 launch_other.go 含 `BuildClaudeArgsForPTY` 等定义需一并确认不与 darwin 冲突（tag 收窄后 linux 用 other，darwin 用 darwin）。

- [ ] **Step 5: 手动验证（外部 Terminal 启动 claude）**

```bash
go run . &
sleep 3
```
在 GUI 用「外部窗口」模式启动实例（LaunchWindowMode=hide/show），选一个目录。
Expected: Terminal.app 弹出新窗口，cd 到目录并启动 claude。

- [ ] **Step 6: Commit**

```bash
git add internal/monitor/launch_darwin.go internal/monitor/launch_other.go
```
用 `git-commit` skill（类型 `A`，描述「macOS 启动实例：osascript 开 Terminal.app 跑 claude」）。

---

## Task 8: 外部终端注入（尽力而为）

**Files:**
- Modify: `internal/monitor/inject_darwin.go`（替换 showExternalWindow/closeExternalInstance 占位）

**Interfaces:**
- Consumes: T6 的 `showExternalWindow(pid) error` / `closeExternalInstance(pid) (string, error)`。
- Produces: 外部实例 ShowWindow 走 activate；CloseInstance 尽力 kill 进程。

- [ ] **Step 1: 替换占位实现**

把 `internal/monitor/inject_darwin.go` 末尾的占位 `showExternalWindow` / `closeExternalInstance` 替换为：
```go
// showExternalWindow 尽力 activate 外部终端窗口（限 Terminal.app / iTerm2）。
// 非保证：若 claude 跑在 VSCode 等内嵌终端，activate 目标不准。
func showExternalWindow(pid int) error {
	// 先尝试 Terminal.app
	if err := exec.Command("osascript", "-e", `tell application "Terminal" to activate`).Run(); err == nil {
		return nil
	}
	// 回退 iTerm2
	if err := exec.Command("osascript", "-e", `tell application "iTerm2" to activate`).Run(); err == nil {
		return nil
	}
	return fmt.Errorf("未找到 Terminal.app/iTerm2 可激活窗口")
}

// closeExternalInstance 尽力终止外部 claude 进程（直接 kill，无法关闭其宿主终端窗口）。
func closeExternalInstance(pid int) (string, error) {
	proc, err := os.FindProcess(pid)
	if err != nil {
		return "", fmt.Errorf("PID %d 不存在", pid)
	}
	if err := proc.Kill(); err != nil {
		return "", fmt.Errorf("终止 PID %d 失败: %w", pid, err)
	}
	return fmt.Sprintf("已终止 PID %d（宿主终端窗口需手动关闭）", pid), nil
}
```
并在 inject_darwin.go import 块补 `"os"`、`"os/exec"`。

- [ ] **Step 2: 验证编译 + 测试**

```bash
go build ./...
go test -race ./internal/monitor/
```
Expected: 通过。

- [ ] **Step 3: 手动验证（外部实例 ShowWindow）**

新开终端跑 `claude`，在 GUI 监控列表点该实例的「显示窗口」。
Expected: Terminal.app 被 activate 到前台（iTerm2 同理）。CloseInstance 会 kill claude 进程（提示需手动关窗口）。

- [ ] **Step 4: Commit**

```bash
git add internal/monitor/inject_darwin.go
```
用 `git-commit` skill（类型 `A`，描述「macOS 外部终端注入（尽力而为）：activate Terminal/iTerm2 + kill 进程」）。

---

## Task 9: 辅助功能（暗色/浏览器/自启/更新）

**Files:**
- Modify(Rewrite): `internal/theme/detect_darwin.go`
- Modify: `internal/monitor/settings_browser.go`
- Create: `internal/monitor/settings_darwin.go`
- Modify: `internal/monitor/settings_default.go`（tag 收窄）
- Modify: `internal/monitor/update.go`
- Create: `internal/monitor/update_darwin.go`
- Test: `internal/monitor/settings_darwin_test.go`, `internal/monitor/update_test.go`

**Interfaces:**
- Produces: `IsSystemDarkMode()`（darwin 真实）；`OpenInBrowser`（darwin open）；`SetAutoStart`/`IsAutoStartEnabled`（LaunchAgent）；`updatePlatformKey()`；`DownloadAndReplace`（darwin dmg）。

- [ ] **Step 1: 写失败测试（plist 生成 + platform key）**

`internal/monitor/settings_darwin_test.go`:
```go
//go:build darwin

package monitor

import "testing"

func TestLaunchAgentPlistContainsRunAtLoad(t *testing.T) {
	xml := launchAgentPlist("/Applications/cc-console.app")
	if !contains(xml, "<key>RunAtLoad</key>") {
		t.Fatal("plist 应含 RunAtLoad")
	}
	if !contains(xml, "open") {
		t.Fatal("plist 应含 open 命令")
	}
}

func contains(s, sub string) bool {
	for i := 0; i+len(sub) <= len(s); i++ {
		if s[i:i+len(sub)] == sub {
			return true
		}
	}
	return false
}
```

`internal/monitor/update_test.go`（若已存在则追加；否则新建）:
```go
package monitor

import "testing"

func TestDarwinPlatformKey(t *testing.T) {
	got := darwinPlatformKey("arm64")
	if got != "darwin-arm64" {
		t.Errorf("darwinPlatformKey(arm64)=%q want darwin-arm64", got)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test -run "TestLaunchAgentPlistContainsRunAtLoad|TestDarwinPlatformKey" ./internal/monitor/
```
Expected: FAIL。

- [ ] **Step 3: 重写 theme/detect_darwin.go**

```go
//go:build darwin

package theme

import "os/exec"

// IsSystemDarkMode 用 defaults read 检测 macOS 暗色模式。
// AppleInterfaceStyle 键仅在用户改过外观时存在；含 "Dark" 为暗色，不存在/其它 = light。
func IsSystemDarkMode() bool {
	out, err := exec.Command("defaults", "read", "-g", "AppleInterfaceStyle").Output()
	if err != nil {
		return false // 键不存在 = 未改过 = light
	}
	s := string(out)
	for _, k := range []string{"Dark", "dark"} {
		if containsStr(s, k) {
			return true
		}
	}
	return false
}

func containsStr(s, sub string) bool {
	for i := 0; i+len(sub) <= len(s); i++ {
		if s[i:i+len(sub)] == sub {
			return true
		}
	}
	return false
}
```

- [ ] **Step 4: 改 settings_browser.go 加 darwin 分支**

读 `internal/monitor/settings_browser.go`，在 `OpenInBrowser` 内加 darwin 分支（需 import `runtime`）：
```go
func OpenInBrowser(url string) error {
	switch runtime.GOOS {
	case "darwin":
		return exec.Command("open", url).Start()
	case "windows":
		// 既有 rundll32 逻辑
	default:
		// 既有 xdg-open / WSL 逻辑
	}
	// ...保留原逻辑
}
```
（把原 `!windows` 的 xdg-open 逻辑放进 `default` 分支，darwin 放 `open`。）

- [ ] **Step 5: 创建 settings_darwin.go（LaunchAgent 自启）**

`internal/monitor/settings_darwin.go`:
```go
//go:build darwin

package monitor

import (
	"fmt"
	"os"
	"os/exec"
	"path/filepath"
)

const launchAgentLabel = "local.cc-console"

func launchAgentPath() string {
	home, err := os.UserHomeDir()
	if err != nil || home == "" {
		return ""
	}
	return filepath.Join(home, "Library", "LaunchAgents", launchAgentLabel+".plist")
}

// launchAgentPlist 生成 LaunchAgent plist 内容。appPath 为 .app 路径或 binary 路径。
func launchAgentPlist(appPath string) string {
	return fmt.Sprintf(`<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key><string>%s</string>
	<key>ProgramArguments</key>
	<array>
		<string>open</string>
		<string>-a</string>
		<string>%s</string>
	</array>
	<key>RunAtLoad</key><true/>
</dict>
</plist>
`, launchAgentLabel, appPath)
}

// SetAutoStart 写入/移除 LaunchAgent。enable=true 安装，false 卸载。
func SetAutoStart(enable bool) error {
	path := launchAgentPath()
	if path == "" {
		return fmt.Errorf("无法定位 ~/Library/LaunchAgents")
	}
	if !enable {
		_ = exec.Command("launchctl", "unload", path).Run()
		_ = os.Remove(path)
		return nil
	}
	appPath := resolveAppPathForAutoStart()
	if err := os.MkdirAll(filepath.Dir(path), 0o755); err != nil {
		return err
	}
	return os.WriteFile(path, []byte(launchAgentPlist(appPath)), 0o644)
}

// IsAutoStartEnabled 检查 LaunchAgent plist 是否存在。
func IsAutoStartEnabled() (bool, error) {
	path := launchAgentPath()
	if path == "" {
		return false, nil
	}
	_, err := os.Stat(path)
	return err == nil, nil
}

// cleanupLegacyAutoStart macOS 无遗留自启项需清理。
func cleanupLegacyAutoStart() {}

// resolveAppPathForAutoStart 返回 .app 路径（优先 /Applications，回退当前 binary 目录的 .app）。
func resolveAppPathForAutoStart() string {
	candidate := "/Applications/cc-console.app"
	if info, err := os.Stat(candidate); err == nil && info.IsDir() {
		return candidate
	}
	// 回退：binary 所在目录上溯找 .app bundle
	if exe, err := os.Executable(); err == nil {
		d := filepath.Dir(exe)
		for i := 0; i < 4 && d != "/" && d != "."; i++ {
			if filepath.Base(d) == "MacOS" {
				app := filepath.Join(filepath.Dir(d), "..") // Contents/MacOS -> Contents/.. = .app? 需修正
				_ = app
			}
			d = filepath.Dir(d)
		}
	}
	return "cc-console"
}
```
（`resolveAppPathForAutoStart` 的 .app 上溯逻辑在 Step 6 验证时按实际安装位置校正；最小可用先返回 `/Applications/cc-console.app` 或 `"cc-console"`。）

- [ ] **Step 6: 收窄 settings_default.go 的 build tag**

把 `internal/monitor/settings_default.go` 顶部 `//go:build !windows` 改为：
```go
//go:build !windows && !darwin
```

- [ ] **Step 7: 改 update.go platform key + 创建 update_darwin.go**

读 `internal/monitor/update.go` 找到硬编码 `m.Platforms["windows-x86_64"]` 处（约 L75），改为按运行时平台选 key：
```go
key := currentPlatformKey()
url := m.Platforms[key]
```
并在 update.go 加：
```go
func currentPlatformKey() string {
	switch runtime.GOOS {
	case "darwin":
		return darwinPlatformKey(runtime.GOARCH)
	case "windows":
		return "windows-x86_64"
	}
	return runtime.GOOS + "-" + runtime.GOARCH
}

func darwinPlatformKey(arch string) string {
	return "darwin-" + arch // darwin-arm64 / darwin-amd64；universal 发布用 darwin-universal
}
```
（import `runtime`。）

创建 `internal/monitor/update_darwin.go`:
```go
//go:build darwin

package monitor

import (
	"fmt"
	"os"
	"os/exec"
	"path/filepath"
)

// DownloadAndReplace 下载 dmg 并打开挂载，提示用户拖拽安装（macOS 无法静默替换 .app）。
func DownloadAndReplace(downloadURL, signature string, onProgress func(downloaded, total int64)) error {
	tmp, err := os.MkdirTemp("", "cc-console-update")
	if err != nil {
		return err
	}
	dmgPath := filepath.Join(tmp, "cc-console.dmg")
	if err := downloadFile(downloadURL, dmgPath, onProgress); err != nil {
		return err
	}
	// 打开 dmg 挂载（Finder 显示），让用户拖到 Applications
	if err := exec.Command("open", dmgPath).Start(); err != nil {
		return fmt.Errorf("打开 dmg 失败: %w", err)
	}
	return fmt.Errorf("已下载并打开 dmg，请将 cc-console.app 拖到「应用程序」替换（macOS 需手动安装）")
}
```
（`downloadFile` 若 update.go 已有同名函数则复用；否则参照 update_windows.go 的下载实现复制一份到 update.go 平台无关部分。验证：`grep -n "func downloadFile" internal/monitor/update*.go`。）

- [ ] **Step 8: 运行测试 + 验证编译**

```bash
go test -race ./internal/monitor/ ./internal/theme/
go build ./...
```
Expected: PASS + 编译通过。

- [ ] **Step 9: 手动验证**

```bash
go run . &
sleep 3
```
- 系统切深色 → app 主题跟随（重开窗口）。
- 设置页点「GitHub」链接 → 浏览器打开。
- 设置页开「开机自启」→ 检查 `~/Library/LaunchAgents/local.cc-console.plist` 存在；关 → 删除。
- 「检查更新」→ 读 latest.json（若 darwin key 无资产则提示无可用更新，符合预期）。

- [ ] **Step 10: Commit**

```bash
git add internal/theme/detect_darwin.go internal/monitor/settings_browser.go internal/monitor/settings_darwin.go internal/monitor/settings_darwin_test.go internal/monitor/settings_default.go internal/monitor/update.go internal/monitor/update_darwin.go internal/monitor/update_test.go
```
用 `git-commit` skill（类型 `A`，描述「macOS 辅助功能：暗色模式/浏览器打开/LaunchAgent 自启/dmg 更新」）。

---

## Task 10: 前端改动

**Files:**
- Modify: `frontend/src/main.js:4582-4586`（openNewTerminal kind）
- Modify: `frontend/index.html`（文案）

**Interfaces:**
- Produces: 前端在 mac 用 shell kind 开终端；文案不出现 PowerShell/CMD。

- [ ] **Step 1: 改 openNewTerminal kind 选择**

读 `frontend/src/main.js` 约 L4582-4586（`openNewTerminal` 硬编码先试 shell 再试 cmd）。改为按平台/系统选 kind：mac/linux 只试 `shell`，失败提示「未找到 zsh/bash」：
```javascript
async function openNewTerminal(workdir) {
  // macOS/Linux：shell（zsh/bash）；不再尝试 cmd
  try {
    return await window.startTerminal('shell', workdir);
  } catch (e) {
    alert('未找到可用 shell（zsh/bash），请检查 $SHELL');
    throw e;
  }
}
```
（实际函数名/调用以 main.js 现有结构为准——`grep -n "openNewTerminal\|startTerminal\|ID_START_TERMINAL" frontend/src/main.js` 定位真实调用，替换 cmd 尝试逻辑。）

- [ ] **Step 2: 改 index.html 文案**

读 `frontend/index.html`，把：
- tooltip `新建终端（PowerShell，失败回退 CMD）` → `新建终端（zsh/bash）`
- 注释 `ConPTY + xterm.js` → `PTY + xterm.js`
- 设置项 `外部窗口调用系统 PowerShell 或 Windows Terminal` → `外部窗口调用系统终端（Terminal.app/iTerm2）`
（用 `grep -n "PowerShell\|CMD\|ConPTY\|Windows Terminal" frontend/index.html` 定位全部，逐一替换。）

- [ ] **Step 3: 重新构建前端**

```bash
cd frontend && npm run build && cd ..
```
Expected: 构建成功，产物更新。

- [ ] **Step 4: 手动验证**

```bash
go run . &
sleep 3
```
- 点「新建终端」→ 开 shell tab（不再尝试 cmd）。
- UI 文案无 PowerShell/CMD 字样。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/main.js frontend/index.html frontend/dist
```
用 `git-commit` skill（类型 `O`，描述「前端 macOS 适配：终端 kind 用 shell + 文案通用化」）。

---

## Task 11: 打包 build-mac.sh（universal + .app + ad-hoc + DMG）

**Files:**
- Create: `build-mac.sh`
- Create: `build/darwin/Info.plist`（模板）

**Interfaces:**
- Produces: `bin/cc-console-<ver>-universal.dmg`，universal，ad-hoc 签名。

- [ ] **Step 1: 创建 Info.plist 模板**

`build/darwin/Info.plist`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key><string>cc-console</string>
	<key>CFBundleDisplayName</key><string>CC Console</string>
	<key>CFBundleIdentifier</key><string>local.cc-console</string>
	<key>CFBundleVersion</key><string>{{VERSION}}</string>
	<key>CFBundleShortVersionString</key><string>{{VERSION}}</string>
	<key>CFBundleExecutable</key><string>cc-console</string>
	<key>CFBundleIconFile</key><string>icons</string>
	<key>CFBundlePackageType</key><string>APPL</string>
	<key>LSMinimumSystemVersion</key><string>11.0</string>
	<key>NSHighResolutionCapable</key><true/>
	<key>LSUIElement</key><false/>
</dict>
</plist>
```

- [ ] **Step 2: 创建 build-mac.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

VER=$(grep 'const Version' service/monitor_service.go | sed 's/.*"\(.*\)".*/\1/')
APP_NAME="cc-console"
APP="bin/${APP_NAME}.app"
DMG="bin/${APP_NAME}-${VER}-universal.dmg"
STAGE="bin/stage"

echo "==> [1/6] 前端构建"
( cd frontend && npm ci && npm run build )

echo "==> [2/6] 生成 icons.icns"
mkdir -p "$STAGE/icon.iconset"
SRC="trayicon.png"
for spec in "16 16" "32 16@2x" "32 32" "64 32@2x" "128 128" "256 128@2x" "256 256" "512 256@2x" "512 512" "1024 512@2x"; do
  set -- $spec; sz=$1; name=$2
  sips -z $sz $sz "$SRC" --out "$STAGE/icon.iconset/icon_${name}.png" >/dev/null
done
iconutil -c icns "$STAGE/icon.iconset" -o "$STAGE/icons.icns"

echo "==> [3/6] universal 二进制（arm64 + amd64 lipo）"
mkdir -p bin
LDFLAGS="-s -w"
GOARCH=arm64 go build -ldflags="$LDFLAGS" -o "$STAGE/cc-console-arm64" .
GOARCH=amd64 go build -ldflags="$LDFLAGS" -o "$STAGE/cc-console-amd64" .
lipo -create -output "$STAGE/cc-console" "$STAGE/cc-console-arm64" "$STAGE/cc-console-amd64"
# slhook 同理
GOARCH=arm64 go build -ldflags="$LDFLAGS" -o "$STAGE/cc-console-sl-arm64" ./cmd/slhook
GOARCH=amd64 go build -ldflags="$LDFLAGS" -o "$STAGE/cc-console-sl-amd64" ./cmd/slhook
lipo -create -output "$STAGE/cc-console-sl" "$STAGE/cc-console-sl-arm64" "$STAGE/cc-console-sl-amd64"

echo "==> [4/6] 构造 .app bundle"
rm -rf "$APP"
mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
cp "$STAGE/cc-console"      "$APP/Contents/MacOS/cc-console"
cp "$STAGE/cc-console-sl"   "$APP/Contents/Resources/cc-console-sl"
cp cmd/slhook/bridge.mjs    "$APP/Contents/Resources/bridge.mjs"
cp "$STAGE/icons.icns"      "$APP/Contents/Resources/icons.icns"
sed "s/{{VERSION}}/$VER/g" build/darwin/Info.plist > "$APP/Contents/Info.plist"

echo "==> [5/6] ad-hoc 签名"
codesign --force --deep --sign - "$APP"

echo "==> [6/6] 打 DMG"
rm -f "$DMG"
hdiutil create -volname "$APP_NAME" -srcfolder "$APP" -ov -format UDZO "$DMG"

echo "完成：$DMG"
lipo -info "$APP/Contents/MacOS/cc-console"
```
赋可执行权限：
```bash
chmod +x build-mac.sh
```

- [ ] **Step 3: 运行打包**

```bash
./build-mac.sh
```
Expected: 6 步全通过，输出 `完成：bin/cc-console-<ver>-universal.dmg`；`lipo -info` 显示 `universal`。若某步失败（如 sips 尺寸、iconutil），按报错修正——常见：trayicon.png 不是正方形，需先用 `sips -z 1024 1024` 规整再切尺寸。

- [ ] **Step 4: 验收安装测试**

```bash
open bin/cc-console-<ver>-universal.dmg
# Finder 打开后，把 cc-console.app 拖到「应用程序」
# 首次启动需放行：
xattr -cr /Applications/cc-console.app
open /Applications/cc-console.app
```
Expected: app 启动，Dock 有图标，托盘有 CC Console。

- [ ] **Step 5: 端到端功能验收（按设计第 12 节验收清单）**

逐项验证：
- [ ] universal（lipo -info）
- [ ] 双击 dmg → 拖装 → xattr -cr → 启动（Dock 图标）
- [ ] 内嵌 claude：监控 + clear/rewind/prompt/ask 注入
- [ ] 桥接：`~/.cc-console/{live,hook,ask}/<pid>.json` 写入（用真实 claude + 桥接 statusline 验证）
- [ ] 开机自启/暗色/浏览器/更新检查
- [ ] 外部实例 ShowWindow activate

- [ ] **Step 6: Commit**

```bash
git add build-mac.sh build/darwin/Info.plist
```
用 `git-commit` skill（类型 `A`，描述「macOS 打包脚本：universal lipo + .app bundle + ad-hoc 签名 + hdiutil DMG」）。

---

## Self-Review 记录

执行人在写完后逐项核对（已预填本计划作者的自审结论，执行时复核）：

**1. Spec 覆盖**：spec 13 节 → Task 覆盖：进程检测(T3) / 桥接+slhook(T2,T4) / 内嵌终端(T5) / 内嵌注入(T6) / 外部注入(T8) / 启动(T7) / 自启(T9) / 暗色(T9) / 浏览器(T9) / 更新(T9) / app.go Mac(T2) / 打包(T11) / 前端(T10) / 环境(T1)。全覆盖。

**2. Placeholder 扫描**：T2 Step 5 的 Mac 选项字段名以编译验证为准（非 placeholder，给了候选 + 兜底）；T9 Step 5 的 resolveAppPathForAutoStart .app 上溯逻辑标注按实际校正。其余步骤均有完整代码。

**3. 类型一致性**：`SessionByPID(pid uint32) (string, bool)` 在 T5 定义、T6 调用一致；`keyTokenBytes`/`renderAskTokens` 在 T6 定义+测试一致；`darwinPlatformKey` 在 T9 定义+测试一致；`actionToken{Key,Text}` 与 inject_windows 一致；`StartPTYSession(kind,workdir,cmdline,cols,rows,onData,onExit) (string,error)` 与 pty_other.go 存根签名一致（已对照 `pty.go`/`pty_other.go`/`inject.go` 逐项核验）。

**4. 架构一致性（本次自检发现并已修正）**：inject 层（monitor 包）需访问 PTYRegistry 实例，但该实例原由 service 层 `NewMonitorService` 私有创建（`monitor_service.go:27`），monitor 包无包级引用。若不处理，T6 的 `writePTY` 要么编译失败要么命中空实例 → 所有内嵌注入静默失效。**已修正**：T5 Step 3 在 pty.go 新增进程级单例 `var ptyRegistry = NewPTYRegistry()` + `GetPTYRegistry()`；T5 Step 6 改 `NewMonitorService` 用 `monitor.GetPTYRegistry()`；T6 Consumes 注明注入层用此包级单例。service 与 inject 现共享同一实例。

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-09-mac-adapt.md`. Two execution options:

1. **Subagent-Driven (recommended)** — 每个 Task 派新 subagent，任务间 review，快速迭代。
2. **Inline Execution** — 本会话内用 executing-plans 批量执行 + 检查点。

Which approach?
